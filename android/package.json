{
  "name": "nenchan-android",
  "version": "1.0.0",
  "description": "Nenchan Android WebView Client",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "dev": "npm run build && npm run cap:sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/browser": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/status-bar": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "typescript": "^7.0.0"
  }
}
